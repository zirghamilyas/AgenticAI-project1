/** Weaviate multi-tenant collection name (schema + all tenant-scoped queries). */
export const AGENT_KNOWLEDGE_COLLECTION = 'AgentKnowledge';

/** Default tenant used by seed scripts and docs; chat requests may use any tenant id. */
export const DEMO_TENANT_ID = 'tenant-demo';
